<article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>{{userPost.typeOfDrone}}</h2>
       <span>{{userPost.tags}}</span>
       <span>{{userPost.description}}</span>
    </div>
  </article>

